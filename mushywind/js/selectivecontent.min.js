function scToArray() {
    var t = [];
    return $(".selectiveContent").each(function () {
        t.push($(this).attr("data-selective-content"))
    }), t
}

function scElement(t) {
    t.each(function () {
        var t = $(this),
            e = t.attr("data-img-src");
        $imgSrcSet = t.attr("data-img-srcset"), t.is("img") && ($imgSrcSet ? (t.attr("src", e), t.attr("srcset", $imgSrcSet)) : t.attr("src", e)), t.show()
    })
}

function selectiveContent() {
    var t = getUrlVars()["hidden_fields.campaign"],
        e = scToArray();
    void 0 !== t ? -1 != jQuery.inArray(t, e) ? scElement($("[data-selective-content=" + t + "]")) : scElement($('[data-selective-content="generic"]')) : scElement($('[data-selective-content="generic"]'))
}